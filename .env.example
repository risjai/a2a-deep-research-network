# Copy to .env (gitignored) or export in your shell.
# Only needed for the Analyst, Critic, and host orchestrator (the LLM agents).
# The Retriever and the test suite run without any key.
# Get a free key at https://aistudio.google.com/apikey
GOOGLE_API_KEY=
